<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
	xmlns:local="clr-namespace:Set;assembly=1Set"
	x:Class="Set.ExerciseListPage"
	Title="{Binding Title}">

	<ContentPage.Padding>
	<OnPlatform x:TypeArguments="Thickness"
	            iOS="5, 20, 5, 0"
	            Android="5, 0, 5, 0"
	            WinPhone="5, 0, 5, 0" />
	</ContentPage.Padding>

	<ContentPage.ToolbarItems>
  		<ToolbarItem Icon="ic_action_new.png" Order="Primary" Clicked="OnAddExerciseButtonClicked" x:Name = "AddExerciseButton"/>
	</ContentPage.ToolbarItems>

	<ContentPage.Content>
	    <StackLayout Orientation="Vertical" >

	      <ListView ItemSelected="OnExerciseSelected" x:Name="exercisesList" HasUnevenRows="true">
	        <ListView.ItemTemplate>
	          <DataTemplate>
	            <ViewCell> 
	            	<ViewCell.View>
	                <StackLayout Padding="10,10,0,0" VerticalOptions="FillAndExpand" Orientation="Vertical">
	                	<Label Text="{Binding Name}" YAlign="Center" FontSize="Medium" />
						        <Label Text="{Binding Notes}" YAlign="Center" TextColor="Gray"/>        
					        </StackLayout>
					      </ViewCell.View>
	            </ViewCell>
	          </DataTemplate>
	        </ListView.ItemTemplate>
	      </ListView>
	      
	    </StackLayout>
	</ContentPage.Content>
</ContentPage>
